#include <stdio.h>
#include <stdlib.h>
#include <time.h>
#include"TAD_lista.h"
//!#include "mergesort.h" -- Ainada  não está adaptado
//#define ARRAYSIZE 10

int main()
{
    /*Lista* li = cria_lista();

    insere_lista(li,1 ,2);
    insere_lista(li,2 ,2);
    insere_lista(li,4 ,1);

    imprime_resultados(li);
    
    libera_lista(li);
    */

    return 0;
}
