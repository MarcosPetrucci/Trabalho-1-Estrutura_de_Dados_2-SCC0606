#include <stdio.h>
#include <stdlib.h>
#include <time.h>
#include"TAD_lista.h"
//!#include "mergesort.h" -- Ainada  não está adaptado
//#define ARRAYSIZE 10

int main()
{
    /*Lista* li = cria_lista();
    Tipo_Dado dt;
    dt.x =1; dt.y =99;
    insere_lista(li,dt);
    dt.x =8; dt.y =99;
    insere_lista(li,dt);
    dt.x =5; dt.y =44;
    insere_lista(li,dt);

    imprime_resultados(li);
    
    libera_lista(li);
    */
    return 0;
}
